Shyland Brief — B5 Housekeeping and Knob Survey — Closeout Report
==================================================================
Date: 2026-07-21
Branch: version_22 (worktree; never merged — merging is the operator's
action). Zero game-code changes; every database interaction was a
read-only query, per the brief's standing rule.

PRE-FLIGHT
----------
Worktree clean and synced; DOCKER_HOST -> production
(games.magrathea.com) with game-mvc-postgres and game-mvc-django
identified among the running stack; gh authenticated with repo access.

STEP 0
------
Brief self-committed verbatim (commit 0f595c1).

STEP 1 — RULING COMMENTS POSTED (verbatim, extracted mechanically from
the committed brief's fenced blocks)
------------------------------------------------------------------
- #100: the five-part B5 ruling set (scope law, +N means +N, stats
  display parenthetical, bar law with the atomic rescale, Armor Option
  C with the open knob numbers).
- #68:  proc semantics — wired minimal honest version, per-item rolls,
  summed bonus, parenthetical display, ranged-proc deferral.
- #109: the reversal — the bankable free heal is dead; bar law governs
  spend; stays OPEN under Version 22 / B5 until the implementation
  ships the refill-line removal.
- #110: fix location — the atomic single-F() rescale inside the B5
  effective-stat / equip-mutation work.

1e MILESTONE CASE: #110 already carried milestone Version 22 — no
action taken (the already-set case).

STEP 2 — ISSUES FILED (hard gate PASSED)
----------------------------------------
- #127: Ranged proc damage ("between X and Y") — new weapon kind,
  midpoint-and-spread family. No labels, no milestone, body verbatim.
- #128: B5 knob survey — armor/proc tuning dataset from code and
  production DB. Milestone Version 22, no labels, body verbatim.
Gate verification: both confirmed via gh issue view — titles, states,
milestones, labels exact; bodies content-identical (GitHub strips
trailing blank lines from issue bodies — whitespace normalization only,
recorded here, not a deviation). Both filed with --assignee @me per the
repo's standing assignment rule (the brief was silent on assignee).

STEP 3 — THE KNOB SURVEY
------------------------
docs/shyland/Shyland_V22_B5_Knob_Survey.json (commit 74303a1; 2,652
lines). Generated by a read-only script executed inside the production
django container (docker cp + docker exec; sys.path /app technique).
Every figure carries a source: repo-relative code paths for
constants/formulas, ORM query strings for production data. All seven
required top-level keys present (meta, equip_slots, armor_definitions,
secondary_stat_census, rolled_distributions, combat_npcs,
player_constants).

Reference-point verification (the brief's "verify, do not trust"
list) — ALL CONFIRMED, no discrepancies:
- SLOT_ORDER = 13 names, RING capacity 2 -> 14 total slots.
- Item/NPC model fields as described.
- combat_utils constants exactly as described (TO_HIT_DEFENSE_BASE 10,
  GRAZE_WINDOW 3, CRIT_BASE 0.05, CRIT_PER_DEX_ADVANTAGE 0.01,
  CRIT_CAP 0.25, NPC_CONTEST_BASE 18, NPC_CONTEST_STEP 2.5,
  NPC_TIER_OFFSET {normal:0, elite:2, boss:2}, MK_LEVEL_SPAN 10,
  multipliers 0.5/1.0/1.5 min 1, vitality_max = END*10 + STR*3 +
  level*5).
- The expected 17-stat secondary vocabulary matched exactly.

Findings of note for the knob session:
- Exactly 8 slots carry armor in reality (CHEST, FEET, HANDS, HEAD,
  LEGS, OFF_HAND, SHOULDERS, WAIST) — the proposed 8-slot weight table
  checks out against the definition data.
- 18 armor definitions; 41 attackable NPC definitions surveyed with
  per-live-Mk-tier effective stats and damage expectations.
- Exactly ONE healing consumable exists (Healing Draught: restore 20 +
  5×Mk scaling, base_value 12).
- get_npc_stats returns a 'vitality' key alongside the contest stats —
  captured as-is in effective_stats.
- Per-round damage expectations use the v21-blessed 55%/45%/45% rates
  with the plain-hit mean, crits/grazes excluded, per the brief's
  #89-survey-style instruction; the formula is embedded per row.
- NPC on-hit effect pools (apply_npc_effects runs after damage) are
  captured per definition as on_hit_effects, citing the tick-engine
  damage path.

DB ANOMALIES
------------
None encountered. No writes were made to any game table.

Post-survey: completion comment posted on #128 with the file path and
commit hash; #128 closed (gate satisfied: JSON committed and pushed).

STEP 4 — THIS REPORT. Nothing removed or pruned.

COMMITS (in order)
------------------
0f595c1  Step 0: self-commit brief
74303a1  Step 3: the knob survey JSON

FINAL COMMIT HASH
-----------------
74303a1 (this closeout and the post-closeout issues report land in
follow-up commits on the same branch).
