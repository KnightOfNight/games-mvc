2026-08-28 — Session start: Shyland V25.10 Brief 1 (Truthful Delivery and the Filing Bot), branch version_25_10.

== Closeout report ==

Brief: docs/shyland/Shyland_V25.10_Brief_1_Truthful_Delivery_And_The_Filing_Bot.md (verbatim at branch tip 5a12655 at session start; no drift).
Technical pre-flight (#252): all 15 §1 claims verified against the code before any work — no mismatches, no hard stop.

Steps landed (one commit per step, pushed at every boundary):
- Step 0  4a29d12  closeout stub (work-has-started signal)
- Step 1  1d65e58  SHYLAND_VERSION -> 25.10-DEV + pin test, same commit; version-start make deploy-dev run
- Step 2  454e37b  door attach singleton (#304): ATTACHED registry, gate order auth -> 4403 -> 4503 -> 4409, guarded release; bot CLOSE_MEANINGS 4409; tests 1-6
- Step 3  14cef97  report family (#306): REPORT_KINDS x4, kind-dispatched a_report, door-composed helpers; bot schema/receipt/prompt kind-aware; tests 7-14 (scenario 15 = v25.8 report tests pass zero-edit)
- Step 4  295e1aa  central runtime directory (#304): ~/.shyland/ for pid/log/convo in sudo_bot.py + botctl.py; module/secrets stay checkout-scoped; no state migration
- Step 5  f1c31d2  store receipts (#305): receipts computed before record; stored answer folds [did: ...] lines; prompt sentence; docstring note
- Step 6  66896ca  sudo files the tickets (#301): file_issue tool + confirm-gate description/prompt, BOT_ACTIONS local execution (actor_name threaded), GITHUB_* constants, provenance footer, structural receipt, botctl GITHUB_TOKEN child-env injection
- Arch    edd6006  architecture doc: stamp 25.10, header hash -> 66896ca, new version paragraph, §4.20/§4.22/§4.23/§4.24 updated

Verification (§10, all passed):
1. make build + in-container suite: 891 tests, all green (877 baseline + 14 new in test_v25_10_brief1.py). Brief's "15 new tests at minimum" note: table scenario 15 (inventory regression) is satisfied by the pre-existing v25.8 report tests passing with zero edits rather than a new test — 14 new tests, coverage of all 15 scenarios.
2. make deploy-dev run after implementation and tests passed.
3. Singleton drill (live dev): worktree dev bot started via botctl; second agent-sudo client drew the already-attached error frame and close 4409; the holder's log was byte-identical before/after (connection untouched); ~/.shyland/.sudo_bot.dev.pid present. Bot stopped; the second client re-run was admitted (hello, protocol 2).
4. Central directory: pid + log (and convo file after first save) under ~/.shyland/; zero state files created under the worktree's agents/.
5. Store receipts (scripted-brain driver over the real dev door): the recorded answer in ~/.shyland/.sudo_bot_conversations.dev.json carries "[did: remembered waypoint 'v2510-e2e' (created, id 15)]" after the text; a receiptless turn stored text alone.
6. Filing refusal: with no GITHUB_TOKEN, a driven file_issue drew {'error': 'not-configured', 'detail': 'no GitHub token on this bot host; filing is disabled'} and a legible scripted decline. Nothing was filed — verification filed no real issue.
7. Report rendering (driver's captured player_message frames on a live admin pane): waypoints — leader 'sudo: 10 waypoints' + one '#id name — Zone: Area: Room' line per row, dangling row not present in store so live paths all resolved; memories — leader 'sudo: 11 memories' + kind-tagged lines with both summary forms ('13 lines' for the bundle); kind-aware receipts 'report (waypoints) to Stubtester: delivered' (no 'on None'). Dangling-room and memory-detail forms covered by tests 9 and 11 in-suite.

Issues closed (gated on verification 1-7): #301, #304, #305, #306 — each with a comment naming this brief and verifying commit 66896ca. Milestone Version 25.10 (#62) issue set closed.

Operator-directed additions (in-conversation, 2026-08-28, beyond the brief):
- botctl's dev target no longer passes --insecure — the renewed wildcard cert is live on dev, so both targets now verify TLS fully (the flag mechanism kept for any future target that needs it). Proven live: dev bot restarted without the flag, logged in and attached cleanly; no --insecure in the running argv.
- The ~/.shyland/ pid and conversation filenames dropped their leading period (sudo_bot.<target>.pid, sudo_bot_conversations.<target>.json; botctl's {name}-generalized forms likewise) — state in a dedicated runtime directory needs no hiding. The dev bot was stopped through old-name code first (the transition discipline), the conversation file renamed to carry history, and the bot restarted: ~/.shyland/ now holds exactly sudo_bot.dev.pid, sudo_bot_conversations.dev.json, sudo_bot.dev.log. Dotted-name references earlier in this report describe verification as it ran at the time.

Playtest finding, fixed in the still-open session (2026-08-29): §11 step 1 exposed a bot-side bug against the brief's own §5 design rule. The door's refusal sends the already-attached error frame BEFORE the 4409 close; the bot's first-frame read expected hello, so its protocol check saw 'protocol None' and took the refuse-to-run FatalError exit instead of the patient backoff §5.2 specifies. Fix: _session treats a pre-hello error frame as a refusal announcement — logs it, waits for the close, and returns to the retry loop, so ConnectionClosed(4409) surfaces with its CLOSE_MEANINGS line. Proven live: a refused new-code duplicate logged 'closed: 4409 — refused: another connection for this account is already attached; retrying in 2s/4s/8s' with the holder untouched. OLD-code bots (the operator's step-1 observation) log the protocol-None fatal and EXIT rather than retrying — the brief's §11 step-1 prediction ('retries quietly' under old code) was wrong about old code, but the behavior is safe (the refused bot stops entirely; no double-acting; the singleton invariant held throughout) and old code is unfixable history. The step-1 checklist expectation is adjusted accordingly. Bot-side only — no game-side change, no rebuild; the dev bot was restarted with the fix.

Playtest finding 2, fixed in the still-open session (2026-08-29): §11 step 2 rendered the new listings as blank rows — the client's report-lines renderer draws dict entries ({'k','v'} or {'segs': [...]}; a bare string renders as an empty line), and the new report kinds sent bare strings (the inventory kind was untouched — its doll/inv sections were already seg-form). Fix: door-side _value_lines wraps every listing/detail row as a value-voice {'v': text} entry; tests updated to assert the dict form (a test-encoding correction, not a spec change — the brief specified line CONTENT, which is unchanged). Suite 891 green; make build + make deploy-dev rerun. The in-suite tests had asserted the door's sent payload, not the client's render contract — which is how bare strings passed verification 7.

Playtest finding 3, fixed in the still-open session (2026-08-29): with the renderer fix live, a repeated "what do you remember" drew a fabricated answer — the bot log shows zero tool calls for the turn (no door report, 0 receipts): the model answered from conversation history, declared the report "in your pane", and echoed the stored [did: report (memories) ...] line into its answer text as if it were a fresh receipt. Two-part fix, both bot-side: (1) structural — machinery strips any model-written line beginning [did: from the answer before delivery and storage (the form belongs to machinery; a model-written one is always a fabrication — the sudo: double-prefix-guard precedent), logged when it fires; (2) prompt — never write [did:] lines yourself, and a request to see a report is an action in the current turn every time (panes are not persistent; a report delivered in an earlier turn is not on their screen now — never answer "already sent"). The dev conversation store was reset (bot stopped, file removed, restarted — the 25.9 poisoning precedent) because the fabricated turn's answer had persisted a false [did:] claim. No game-side change.

Playtest finding 4, fixed in the still-open session (2026-08-29): the fabrication recurred intermittently even under the finding-3 prompt orders — the log shows a one-call end_turn turn with zero tools ("Sent — full memory list delivered to your pane.", strip guard fired, 0 receipts): the model pattern-completes 'Sent' answers from replayed report history without acting; prompt discipline alone is demonstrably insufficient. Structural fix: the strip guard grows into a REDO GATE inside the tool loop — a would-be final answer containing a [did:] mark while machinery holds no receipts is a machine-detected fabrication (the mark is the lie's signature) and is bounced back into the loop with a corrective user message: the model performs the real call or drops the claim (bounded by TOOL_LOOP_CAP; exhaustion falls through to the strip guard). Proven live over the real dev door with a scripted brain as agent-smith: fabricated turn bounced (corrective observed in model history), real report call made on the retry, stored answer carrying the genuine machinery receipt with the fabrication gone. Residual gap, machinery-invisible by construction: a fabricated claim that writes NO [did:] mark (plain 'Sent - ...' prose, zero receipts) cannot be machine-detected without classifying prose. Observed live during the playtest (a second occurrence, unmarked this time) and FILED as #308 on operator direction (2026-08-29) — the #304/#305/#306 design-lane pattern from 25.9; candidate rulings on the ticket (deterministic routing of report-shaped requests, zero-tool-turn bounce, tool_choice forcing). Additional dev leaving: throwaway dev-DB password set on agent-smith for the proof driver (its .secrets file untouched).

Deviations: none from the brief's specification. Notes:
- New-test count is 14, not 15 — see verification 1 (scenario 15 is a zero-edit regression pass by design, not a new test).
- botctl's github_token_file (§9.1) landed in the Step 6 commit rather than Step 4's, keeping step-to-issue mapping clean; content exactly as specified.

Transition note (§7.4, recorded as directed): a bot already running was started by old code and owns an old-location (per-checkout) pidfile — the new botctl cannot see it. Before starting a new-code bot on a target, stop any running bot for that target FROM THE CHECKOUT THAT STARTED IT. If missed, the door's 4409 is the backstop: the new bot logs 'already attached' and retries instead of double-acting. State at this session's end: no old-code dev bot was running (nothing to stop); the PROD bot is running old code from the main checkout (pidfile /Users/ctg/src/games-mvc/agents/.sudo_bot.prod.pid) and was not touched — after the eventual prod deploy, the operator's standing restart must stop it from the main checkout and start it with post-25.10 code so its state lands in ~/.shyland/.

Dev-stack leavings (operator prunes; nothing deleted by this session):
- agents/.secrets and agents/venvs symlinks created in this worktree pointing at the main checkout (the 25.9 convention).
- Throwaway password set on dev account sudo-e2e (character Stubtester) for the pane-capture driver; value in /tmp only.
- Waypoint 'v2510-e2e' (id 15) taught into agent-sudo's dev store by the driver.
- Old per-checkout bot state files (pid/log/convo) from pre-25.10 code abandoned in place per the brief.
- The dev bot is RUNNING from this worktree (post-25.10 code, ~/.shyland state, filing=disabled) — ready for the operator playtest. Playtest prerequisite for step 5 of §11: create agents/.secrets/github-token.sudo (operator).

PENDING DEPLOY-TIME ACTIONS: none. No migration (code-only release), no seed, no prod-side data action. Operator-side notes for the record (neither a deploy-time block): (a) prod bot restart after the eventual prod deploy is the operator's standing action — restart with post-25.10 code, stopping the old bot from the main checkout that started it; (b) prod filing needs agents/.secrets/github-token.sudo on the bot host (executor: explicitly the operator), any time before first use.

Suite count: 891 green in-container. Final content commit: edd6006 (this closeout-report completion commit follows it).

Operator playtest disposition: [recorded at session end]
