Amendment 1 complete. Closeout report:

Part 1 — issues filed (all gate checks passed):
- A = #71 (header name, bug) — https://github.com/KnightOfNight/games-mvc/issues/71
- B = #72 (stat bars + band gauge, no label per brief) — https://github.com/KnightOfNight/games-mvc/issues/72
- C = #73 (viewport fit, bug) — https://github.com/KnightOfNight/games-mvc/issues/73
- D = #74 (bulk repair splitting, bug) — https://github.com/KnightOfNight/games-mvc/issues/74
- All four verified open with milestone Version 20, assignee, correct labels, full bodies (byte-diff clean on the longest); #51 pre-verified OPEN with the right-pane scrollbars title.

Part 2 — implemented (deployed live via make build — all three games bounced again): verbatim character_name in every state-sync payload + header with no text-transform; V/L ratio bars and the 0.0–2.0 Acuity band gauge with server-supplied band bounds; page-never-scrolls viewport fit (html/body height:100%; overflow:hidden chain, right pane overflow:hidden); repair-all split per the #63 rule.

Emitter audit (all-vintage, complete):
- Fixed here: repair all (per-attempt lines — success/failure/afford-stop — plus separate summary).
- Fixed previously: bulk sell (#63, Brief 3 Amendment 1), loot sweep/union loot (#62).
- Exempt by ruling: buy N (single atomic transaction).
- Verified clean — already per-event: corpse-decay broadcasts, level-up messages (one per level), tick-engine combat lines.
- Verified clean — single events with enumerating riders: death's broken-items list, effect riders appended to a hit line, equip/unequip blocker refusals (_join_owned_names).
- Verified clean — reports/renderings (single messages by design, #56): inventory, wallet, who, help, stats, vendor list, examine, travel listing, room render (players/exits/ground), usage/disambiguation lines.

Part 3 results: 97 container tests green (2 new: verbatim mixed-case name, per-character band bounds). Live 10/10 WebsocketCommunicator checks: six separately stamped repair lines (three real failures as own lines), one separate summary, all system category with ts, seq strictly monotonic 1→14, character_name verbatim (MiXedCaseRepair), band bounds in every status. Origin band diversity confirmed server-side (Voidtouched 0.4–1.3 vs. others). Map renderer: zero diff lines (byte-identical). Viewport/no-scrollbar and bar-visual checks at the four widths are yours in the browser — the fix makes page scroll impossible by construction, but the visual pass is the operator's per CLAUDE.md.

Parts 4–5: Architecture doc updated in place (§4.15 stats-pane + viewport-fit rule; §4.14 bulk-op note extended with the completed audit). Issues report Shyland_Issues_Report_20260715T191106Z.md committed.

Commits: implementation + doc 56a52e9, issues report c5478ee (final) — both pushed on the PR #69 branch. #71, #72, #73, #74, and #51 are all closed with comments referencing this amendment (#51's notes it was resolved by C's viewport fix).

result: Brief 4 Amendment 1 applied and live — filed+closed #71/#72/#73/#74 plus #51 (header name, stat bars/band gauge, viewport fit, repair splitting + complete emitter audit); commits 56a52e9/c5478ee on PR #69.
